# Report — Agent-ize the terminals (TASK-AGENTS, docs-agent tranche 3)

**Session:** docs-terminal (Opus), resuming an interrupted predecessor mid-tranche.
**Charter:** `docs/TASK-AGENTS.md`. **Date:** 2026-07-18.

## TL;DR

The six standing agents now exist as user-level definitions in `~/.claude/agents/`,
the operating loop is written down and dual-home-ready, and the docs charter now
owns agent-file upkeep. **One blocker:** this session has **no Linear MCP
connected**, so I could not file the six per-agent issues — they're drafted
ready-to-file below and flagged for the lead. Nothing else outstanding.

## Adopted from my predecessor (never reverted)

The interrupted session had already written **four** agent files (timestamps
00:40–00:41): `data-agent.md` (fable), `docs-agent.md`, `quality-agent.md`,
`review-agent.md`. I read all four, matched their structure and voice, and left
them intact. I authored the remaining two.

## Shipped

### Agent definitions (`~/.claude/agents/*.md`, user-level / cross-repo)

All six present, well-formed frontmatter, each under the ~120-line identity budget,
models per the founder's budget (fable = lead + data; opus = the rest):

| File | Lines | Model | Author |
| --- | --- | --- | --- |
| `data-agent.md` | 74 | fable | predecessor |
| `docs-agent.md` | 72 | opus | predecessor |
| `quality-agent.md` | 72 | opus | predecessor |
| `review-agent.md` | 67 | opus | predecessor |
| **`ui-agent.md`** | 80 | opus | **this session** |
| **`lead-agent.md`** | 68 | fable | **this session** |

Both new files carry the founder's full eight-part checklist (mission+specialty,
read-first education, repo-portable house rules, seam protocol, verification
standard, Linear protocol, report protocol, escalation). `ui-agent` guards **both**
design systems (Liminal's 44-primitive teal/navy kit + 44B's editorial/paper
palette) with the "one law" (reuse; a new primitive is a deliberate, announced
act) and the table-overflow / catalog-upkeep duties. `lead-agent` is the condensed
orchestrator identity verbatim from the sitting lead — writes briefs+kickoffs,
reviews every report, rules on every flag, conserves context, arbitrates seams,
budgets models, surfaces founder-calls.

These files live **outside** the repo (in `~/.claude/`), so they are on-disk only —
not part of the git commit below. That's intended: they're user-level so they
redeploy to 44b and hq.

### `docs/OPERATING-MODEL.md` (new)

The loop written down: the cast + model budget, the brief→kickoff→execute→report→
review→redirect cycle (with an ASCII diagram), the conventions (tranche numbering,
disjoint granted seams, Linear-as-shared-brain, verification-means-exercised), the
Monitor pattern, the kickoff-line template, and the standing decisions. Seeded from
memory `liminal-operating-model` and dual-home-ready (repo = source of truth; a
Linear Document is the mirror, once MCP is back — see blocker).

### `docs/TASK-DOCS-ENGINE.md` (edited)

Added **agent-file upkeep** to the docs charter: `~/.claude/agents/*.md` +
`docs/OPERATING-MODEL.md` are now in the "What you own" list, and a new "Agent
definitions" refresh procedure states the key rule — identity files evolve through
the **same review loop as code** (lead-briefed, executed, reviewed, recorded), not
ad-hoc edits; docs-agent does *upkeep*, authoring a new role is a lead-briefed task
like TASK-AGENTS itself was.

## Verification

- All six agent files: `head -1` is `---` (valid frontmatter), line counts 67–80
  (all under the 120 budget), `model:` present on each (fable on data+lead, opus on
  the other four). Verified by direct read, not just a glob.
- `OPERATING-MODEL.md` and the `TASK-DOCS-ENGINE.md` edits: authored and re-read in
  context; the roster table and model budget match the six files on disk.
- I did **not** touch the two working-tree files owned by the data terminal
  (`docs/MRF-INDEXES.md`, `docs/data/DATABASE.md`) — out of my seam, left as found.

## BLOCKER — Linear issues could not be filed

TASK-AGENTS asks for one NYS-100-style issue per agent created. **This session has
no Linear MCP server configured** (`claude mcp list` → none; no `.mcp.json`; the
WAF blocks raw `curl … Bearer`, so an API key wouldn't be a reliable fallback). The
predecessor docs-linear session *did* have Linear MCP; this one does not. I did not
fabricate filings. The six records are drafted ready-to-file:

1. **data-agent — supply-side acquisition agent** (Data Engine). The standing
   identity for MRF/TiC + FHIR + reference-layer harvest and psql-COPY loaders;
   relate to the harvest milestones.
2. **quality-agent — data-trustworthiness + surfaces agent** (Data Engine).
   Matview/repo correctness, measure-before-port forensics, premise-checking, thin
   product surfaces; relate to NYS-65/88/115.
3. **docs-agent — institutional-knowledge agent** (no project / Ecosystem).
   Linear structure + the three living documents + agent-file upkeep; portable to
   44b.
4. **review-agent — adversarial review agent** (no project / Ecosystem).
   Findings-only review of a named commit range; relate to the review reports.
5. **ui-agent — dual design-system guardian** (**Leuk**). Reuse-the-kit law,
   adherence sweeps, primitive promotion + catalog upkeep across Liminal + 44B.
6. **lead-agent — orchestrator** (no project / Ecosystem). Briefs, kickoffs,
   reviews, rulings, seam arbitration, memory; model fable.

## Flags for the lead (rulings requested)

1. **Linear MCP** — connect it to a docs-terminal session (or hand me a session
   that has it) and I'll file all six at NYS-100 quality + create the
   `OPERATING-MODEL` Linear Document mirror. Until then those two deliverables are
   parked, not done.
2. **New "Ecosystem" project?** TASK-AGENTS left this to your approval at review.
   My reversible default was to place ui→Leuk, data/quality→Data Engine, and
   lead/docs/review → no-project. If you'd rather have a dedicated **Ecosystem**
   project to hold the meta-work (the agent roster, OPERATING-MODEL, future
   identity revisions), say so and I'll create it and move the three no-project
   issues into it when MCP is back.

## Next-tranche suggestions

- Once MCP lands: file the six issues + mirror OPERATING-MODEL to a Linear
  Document, then this task is fully closed.
- A first **live exercise** of the loop: have the lead spawn one of the new agents
  via the Agent tool with a one-line kickoff against a real `TASK-*.md`, to prove
  the identity/tranche split works end-to-end (not just on paper).
- Consider a periodic **agent-file review** in the same monthly cadence as the
  code-review→tech-debt chain, so identities don't drift from the invariants they
  cite (renamed docs, changed house rules).
