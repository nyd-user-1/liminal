# 2026-07-18 — Entities tranche (data-agent)

The entity layer that unblocks Find-my-plan, shipped and verified live, plus
the re-prioritized NYS-143/144 follow-ons and the P2 verifications. Commits:
`8dffd02` (insurer + network layer), `43b5828` (NAIC backbone), `c83a526`
(full disposition + glossary). Nine tickets closed with evidence:
NYS-48, 49, 104, 143, 144, 110, 16, 40; NYS-53 state-corrected by comment.

## P1 — the entity layer (sql/042–046)

**sql/042 `dfs_insurers` + `scripts/ingest-dfs-insurers.mjs` (NYS-104).** The
DFS portal's Liferay shell fronts a plain JSP app; nine org-type POSTs return
the full supervised-company lists (no pagination, no auth). 242 entities
loaded — every row with NAIC code, 240 with FEIN, 162 with NAIC group. The
list is the Rosetta Stone the founder called it: parent ownership
("671/Elevance Health Inc.") and the carrier's own EIN come straight from
the regulator, so no name-matching exists anywhere in the layer. Found along
the way: Cigna's NY entity is licensed as a LIFE insurer; Fidelis's legal
name is "New York Quality Healthcare Corporation"; Excellus holds two DFS
licenses (X4289 HMO + 55107 MHL, same FEIN).

**sql/043 `insurers` + `insurer_aliases` (NYS-48).** 47 canonical rows
(7 groups, 36 carriers incl. Oscar, 4→6 administrators) with parent
self-reference. One crosswalk for every vocabulary: all 33 MRF payer labels,
all 12 source slugs, all 3 billing payers, 37 NAIC codes, 11 NAIC group
codes. `insurer_unmapped_labels` (the tripwire view) reads ZERO on live
vocabularies. 22.6M covered lives resolve through the Form 5500 NAIC leg.

**sql/044 `networks` + `network_aliases` (NYS-49).** Canonical networks keyed
to insurer + optional administrator (Carelon/Optum/Evernorth/MagnaCare/
MultiPlan). Exact-label aliases only — never fuzzy. **99.35% of the 13.4M
rate rows resolve**; the headline unlock: **Evernorth Behavioral merges
9,402 NPIs corroborated by BOTH federal sources** where the exact-name join
was zero. Repo wiring of `listNetworkMemberships` left to the product seat
(join spec in the NYS-49 close).

**sql/045 `insurer_companies` + role (NYS-143).** The NAIC company grain
under the brands: 35 companies / 16 brands, naic + ein + license_type +
group per row. HMO-as-license confirmed in data (group 707 = UHC-NY [AH] +
UHC of NY [HMO] + Oxford Ins [AH] + Oxford Plans NY [HMO]). `role` sits on
the label, not the company — Aetna Life is insurer under its own label,
administrator under 'Aetna (Healthfirst TPA)'. Schedule A health rows:
36,685 resolve via NAIC + 400 more via the EIN leg.

**sql/046 `payer_network_map` + glossary (NYS-144).** All 1,133 raw FHIR
networks dispositioned (first-match rules, rule stored): ny-commercial 35 ·
national-commercial 56 · oos-commercial 355 · medicare 320 ·
oos-government 104 · employer-custom 96 · ny-government 17 · ancillary 4 ·
unclassified 146 (state-ambiguous bare labels, honestly unresolved).
**Real canonical count: 69 networks, all in use.** Evidence against
wholesale merging: exact-roster identity collapses only 1,133→951 — the
five FHIR feeds are national, and most raw rows are simply not NY networks.
Over-merge guardrails held (per-state EBH kept separate; only the founder's
own decompositions folded: Pathwell = program, PCP-Required = plan design).
Glossary: `docs/data/ENTITIES.md` — insurer/product/plan/network/provider+
rates, "book" retired.

## P1c — mints that became loads (NYS-110 + the IHNY/EHPI bonuses)

**MetroPlus premise correction: they never stopped publishing — their web
page did.** Date-probing the Carelon Azure container (2,676 HEADs; listing
disabled, names guessable) found 58 monthly editions the frozen page never
listed, current pair 2026-07-05. Loaded ad-hoc same day with the wide
20-code set, superseded 2024-02-07 QHP/GoldCare rows deleted in one
verified transaction. **10.1% of matched rate pairs genuinely repriced**
(26,104 of 258,492) — correctness with teeth. Second correction: the FFS
series ended 2023-10-06 for real (probe-proven), so the ticket's "5,218
NPIs to 2026" was structurally impossible — QHP 3,747 + GoldCare 3,092
moved; FFS 2023 retained as the newest edition in existence. Probe recipe
recorded in `docs/MRF-INDEXES.md`.

**Independent Health is now a live payer** (NYS-29 crack): 4 egress files,
164,567 rows, 2,048 NPIs, **+92 net-new coverage**. **EmblemHealth EHPI**
(second Emblem legal entity, found on the INN listing): 103,823 rows /
6,562 NPIs via the reference parser after scan-tic's defensive abort fired
correctly on a Carelon layout quirk (name-before-billing_code beyond the
4KB head window; URL quirk: the file lives under `FileType=Previous`).
All new labels pre-seeded into the alias tables — the tripwire stays clean.

**Corpus: 13,668,068 rows · coverage 47.88%** (50,996 / 106,512).

## P2 — verified, closed, corrected

**NYS-16 (Oxford): closed on measurement, not a re-run.** The "40 files /
48GB" = ~13 products × 3 byte-identical entity stamps; every
behavioral-relevant product already loaded at the 2026-07-01 vintage.
Measured: the ~30GB of medical-network files already scanned add only
**20 NPIs** over the OHBS/P3 behavioral panel (16,156 of 16,176 family NPIs
ride behavioral). The real gap was code WIDTH (all UHG rows = old 5-CPT
set): **`2026-07-18-wide-uhc-oxford.txt` queued** (8 files, ~35.6GB, stable
URLs) for tonight's 01:04 run.

**NYS-40 (discovery): near-exhausted by construction.** 304,716 raw
unmatched → 277,430 distinct → license cut **0** (NYS-26's NPPES-wide
ingest already owns the whole NY-licensed behavioral universe — this also
covers never-captured books) → location cut **15** stragglers, all
ingested via the July NPPES monthly allowlist stream (9.67M rows, 15/15).
**Book: 106,497 → 106,512**; `npis.txt` regenerated so tonight's scans
include them.

**NYS-53 premise correction:** the brief's "#1 sub-issue needs no new API
calls" was stale — NYS-54–58 all closed 2026-07-15 and their tables are
verified live (qualifications 99,511 · locations 44,916 · services 56,646 ·
affiliations 2,664 · plans 809). Resource coverage is 6-of-7; only NYS-59/60
(both Low) remain. Commented, left open for the lead.

## Open items / handoff

- **Tonight's queue**: `2026-07-18-wide-uhc-oxford.txt` (code-width rescan,
  16.2k NPIs gain ~15 codes of depth). Runner armed, no lock, launchd loaded.
- **Matviews**: base tables carry today's loads; the 04:12 cron (or the
  runner's post-load rebuild) refreshes the app-facing views. No manual
  rebuild run — the launchd daily-rebuild fired at 17:08 before the loads.
- **MetroPlus monthly**: the blob pair refreshes on the 5th — a candidate
  for a recurring jobs.json entry (ops-agent's call; recipe in MRF-INDEXES).
- **Emblem EHPI vintage**: 2026-05-01 is EHPI's newest posting (EHIC's is
  2026-06-05); recheck the INN listing next Emblem pass.
- **Unclassified tail (146)**: state-ambiguous bare labels in
  `payer_network_map` — curate opportunistically via
  `network_unmapped_labels` weights; nothing NY-load-bearing verified in
  samples (one 2-member "NY Hotel Trades" custom row noted).
- **Find-my-plan joins ready**: employer EIN → form5500 Schedule A →
  naic/ein → `insurer_companies` → brand; plan/product label → network
  alias → canonical network → rate rows. The product seat has everything.

STOP.
