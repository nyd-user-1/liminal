# 2026-07-18 — Ops terminal: three harvestd health bugs

Brief: fix NYS-117 / NYS-119 / NYS-124 (smallest first), verify each live, close
each with evidence, report, STOP. Seam: `ops/harvest/**` + `lib/repos/sync-runs.ts`,
nothing else. Shipped in one commit **`9dfdb22`** + migration **`sql/041`**
(applied to prod, pushed to `main`, clean fast-forward `ecacf1e..9dfdb22`).

## What shipped

| ticket | fix | where |
|---|---|---|
| **NYS-117** | `manifestJobs()` refuses any stem outside `^[A-Za-z0-9._-]+$` before it reaches the `bash -c` string; rejected manifest is logged and left in `queue/`, never run | `ops/harvest/runner.mjs` |
| **NYS-119** | runner records each job's kill-timeout (`timeout_ms`, new col) at open; repo judges `died` against `timeout_ms + 10m grace`, flat 30m only as the fallback for the daily cron / legacy rows | `runner.mjs` + `sync-runs.ts` + `sql/041` |
| **NYS-124** | a success far faster than the job's own history is ledgered `status='suspect'`, its manifest kept in `queue/`, and it raises the email + bell + non-zero exit | `runner.mjs` + `sql/041` |

**NYS-124 guard, precisely:** on exit 0 the runner compares duration to the
job's history — **baseline** (< 20% of the median of the last ≥2 `ok` runs;
suspect/error excluded so a bad run can't poison it) or, before a baseline
exists, an MRF-only **15s floor** (a curl→scan→load can't finish faster; scoped
to manifest jobs so `rates-rollup`'s seconds never trip it; Fidelis's real ~31s
book clears it ~2×). `state.json` records `suspect` (not `ok`) so a `once`
manifest stays due next night instead of being stranded.

Also: `runner.mjs`'s main loop now runs under an entry-module guard so the three
helpers (`manifestJobs`, `suspectFastSuccess`, `baselineMs`) are importable —
which is how each fix was exercised against the real shipped code, not a copy.

## Verified live (not "exited 0")

Against the live Neon DB, exercising the actual functions; all synthetic rows
inserted and removed, ledger confirmed back to its exact prior state
(10 `ok` + 1 `error`, 0 `suspect`, 0 leftovers):

- **NYS-117** — 10 shell-metachar stems rejected (`; rm -rf x`, `$(whoami)`,
  `` `id` ``, `a b`, `a;b`, `a|b`, `a&b`, `a>b`, `a*b`, `a$x`), 5 legit stems
  accepted (`emblem.v2`, `stream-testp`, `2p-testp`, …), no metachar in any job id.
- **NYS-124** — baseline: 3s vs a ~1000s history → suspect; a normal-length run
  → not; floor: fresh MRF harvest 3s → suspect, but a fast **report** job → not
  and a 31s book → not; **the actual Emblem incident row (2.7s) → suspect.**
  (22/22, shared suite with NYS-117.)
- **NYS-119** — through the real `recentSyncRuns()`: 45m into a 600m timeout →
  **running** (the false-red this ticket is about); 700m in → **died**; 40m with
  no `timeout_ms` → **died** via the 30m fallback; 5m into a 30m timeout →
  **running**. (4/4.)
- `npx tsc --noEmit` clean — the repo change compiles and, because the
  `SyncRun["state"]` union is deliberately unchanged, the two `/insights` app
  components still typecheck.

## Fleet status

- **Last night (2026-07-18):** ledger holds the five NY-book harvests + the
  weekly/report jobs — 10 `ok`, 1 `error`. The Emblem 2.7s false-`ok` that a
  data agent caught by hand is now caught by mechanism.
- **Timing chain intact:** untouched — runner 01:04, matview cron 04:12 ET. No
  schedule changed.
- **Tripwires:** `queue/` **empty** (0 manifests → no signed-URL-expiry risk);
  **no missed nights** in the ledger; matviews not in scope this tranche.

## Migration sequencing (my ledger)

- Handed **`sql/041`** to myself (`sync_runs`: `+timeout_ms`, `status` CHECK
  gains `suspect`). Fully idempotent (`ADD COLUMN IF NOT EXISTS`,
  `DROP/ADD CONSTRAINT IF EXISTS`) — safe that it fills the 040→050 gap and thus
  applies after the already-live `050_cpt_codes_wide`; the DDL is order-independent.
- **Heads-up for the next sequencer:** an untracked `sql/060_search_trgm_indexes.sql`
  is sitting in the working tree from a concurrent session — not mine, left
  alone. Current high-water = 060 (pending). Next free below it: 042–049, 051–059.

## Flags

1. **`suspect` renders red on `/insights`** — the DB mark is honest and distinct,
   but the repo maps it to `state:"error"` (red) because the `SyncRun["state"]`
   union is UI-owned (outside my seam). A first-class amber "Suspect" badge is a
   ~4-line follow-up for **ui/quality** (add the arm to the `Record<SyncRun["state"]>`
   maps in `sync-health.tsx` + `run-history.tsx`). A suspect run needs a human
   either way, so red is not wrong in the interim.
2. **NYS-124 has no project/milestone** — its siblings sit in Data Engine →
   "Watched pipelines"; NYS-124 is loose. Filing note for **docs-agent** (Linear
   structure is their seam).
3. **Concurrent uncommitted tree** — another session has `/insights` components,
   `lib/repos/rate-rows.ts`, `sql/060`, and `scripts/_tmp_verify_*.mjs` in flight.
   I staged only my three files; nothing of theirs was touched or pushed.

## Next-tranche suggestions

1. **Complementary `rows>0` assertion in the loader** (data-agent's seam,
   `scripts/mrf/load-rate-signals.mjs`): my duration guard is the ops-layer catch,
   but a first-run false-success that happens to take >15s would still slip it — a
   row-count assert closes that. Defense in depth; the data terminal already
   flagged wanting this (T2 report §1b, next-list #2).
2. **Record loaded row-count into `sync_runs.steps`** so the guard can test "0
   rows" directly rather than inferring from duration — strictly stronger, but
   needs the loader to report counts (data-agent seam).
3. Ship flag #1 (the amber Suspect badge) so the ledger's honesty is visible on
   the card, not just in the row's `error` text.

Queue empty, nothing in flight, no runs racing. STOP.
