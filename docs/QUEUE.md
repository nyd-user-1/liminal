# Lead queue — warm assignments + pending Linear intents

Maintained by the lead (see OPERATING-MODEL.md → Linear governance).
Last sync: 2026-07-18 evening.

## Warm assignments

- **ui-agent (or founder-driven Opus/Sonnet terminal)** — TASK-PAYR-PAGES
  (brief + kickoff ready; reference shots in docs/reference/rate-intel-inspo).
  Behind it: the huge UI backlog — NYS-147 restart (small increments,
  founder sign-off per item), NYS-148 record shape, NYS-127 /directory port,
  NYS-134 public /search.
- **data-agent** — idle. Next: MetroPlus monthly recurrence (jobs.json,
  ops call), Form 5500-SF load (multiplies the census-match universe),
  Anthem/Empire finish (+833 NPIs, signed URLs valid to ~2026-08-19).
- **ops-agent** — idle-green. Belt live pending founder's DATABASE_URL
  secret; NYS-132-style honesty for run-stream.sh/run-two-pass.sh open.
- **quality-agent** — NYS-133 directory search reorder; suspect-badge
  (amber) on /insights.

## Pending Linear intents (file at next end-of-session sync)

- NEW issue (Data Engine, epic): "Rate data → sellable API" — from
  docs/RATE-API-ROADMAP.md (lead-corrected grain/SQL/refresh). Not yet
  briefed; needs founder priority call vs the coverage backlog.
