# HANDOFF — TASK-TABLE-STANDARD — COMPLETE

_Started as a mid-session handoff (account was about to hit its usage window) — the task finished before the cutover happened, so this doc is now a record of what was done rather than a resume point. Left in place in case anything below needs revisiting. Read `docs/TASK-TABLE-STANDARD.md` first — that's the spec._

## Status: all 8 sections done

`npx tsc --noEmit` is clean (final run, zero errors). **Not verified in the browser** — deliberately skipped per this session's standing instruction (see "Working style" below); Brendan checks the dev server himself. **Nothing has been committed** — `git diff --stat` shows 11 files, +694/-475, all uncommitted. Confirmed via `git log`/`git diff HEAD` that concurrent-session commits landed mid-task (`2a979bc`, `6c7cd83`, `b974df7`, `50f15f7`) without conflicting with or duplicating any of this work — checked explicitly, see the Billing section below for the one file (`insurer-mark.tsx`) that overlapped in scope but not in content.

Sections 5–8 (Billing, Portal sweep, Verify+report) below were originally written as a forward plan before they were executed — they now read as "planned → done," kept as-is since the plan was followed exactly.

### Done
1. **`components/ui/table.tsx`** (the primitive — read this file first, everything else depends on it):
   - Header row recolored: `bg-surface`/`text-text-muted` → `bg-primary-wash`/`text-primary` (teal, 14/600). Stays opaque under `stickyHeader` automatically (unchanged mechanism — fill sits on `th`).
   - New exports, all colocated in this file per the doc's "implement once" instruction:
     - `useSort<Col extends string>(initial)` → `[sort, toggleSort]`. Click a column → asc; click again → desc; click a different column → asc.
     - `SortableHead<Col>({label, col, sort, onSort, className?})` — pass as a `head` array entry (it's just a `ReactNode`). Renders a button with a chevron (`Icon` from `@/components/ui/icons`, `chevron-up`/`chevron-down`), faint when inactive. `whitespace-nowrap` baked in.
     - `useSentinel(onIntersect, enabled)` → ref to attach to a sentinel `<tr>`. Uses a ref-swap trick internally so the callback is always fresh without re-subscribing the IntersectionObserver on every render (only re-subscribes when `enabled` flips).
     - `useLazyBatch<T>(items, {batchSize=50, resetKey})` → `{visible, hasMore, sentinelRef}`. Client-side batch growth over an already-filtered/sorted array. Pass `resetKey` (e.g. a joined string of active filter values) to snap back to the first batch when filters change.
     - `LoadMoreRow({sentinelRef, colSpan})` — the "Loading more…" `<tr>`, render as the last child inside `<Table>` when `hasMore`.
   - **Important layout gotcha found and fixed everywhere it appeared**: for `stickyHeader` to actually create an internal scroll (not the whole page), the `Table` needs `className="min-h-0 flex-1"` — **not just `min-h-0`**. `min-h-0` alone was a latent bug on the Clients page (it only had `min-h-0`, not `flex-1`, meaning the table wouldn't actually get a bounded height in the flex column and the page would've scrolled instead of the table). Grep for `className="min-h-0"` (no `flex-1`) if anything still needs this fix.
   - Standard page shape everywhere this is used: root `<div className="flex h-full min-h-0 flex-col">`, filter/toolbar rows `shrink-0`, `<Table className="min-h-0 flex-1" stickyHeader ...>` as the last flex child.
   - Equal-margins fix (doc item 6): toolbar-to-table gap changed from `mb-4` (16px always) to `mb-4 md:mb-6` (16px mobile / 24px desktop) to match `AppShell`'s `p-4 md:p-6` bottom padding on `<main>`. Applied per-page, not globally (didn't touch `app-shell.tsx` — too broad a blast radius for a page-scoped ask).

2. **`app/(app)/clients/clients-index.tsx`** — fully done:
   - Client name cell: `TextLink` now has `className="!font-medium"` (the `!important` is deliberate — `wipe` variant's `font-semibold` and a plain `font-medium` are equal-specificity Tailwind utilities, so a plain override isn't guaranteed to win; `!font-medium` forces it). Per the doc, did **not** touch the `TextLink` primitive itself.
   - Added sortable **Created** column (`formatDate(c.createdAt)` — `Client.createdAt` already existed on the type/repo, already ISO-normalized, no repo change needed).
   - Name/Created/Status headers sortable via the new `useSort`/`SortableHead`.
   - Replaced the manual `limit`/`IntersectionObserver` code with `useLazyBatch(sorted, {resetKey: \`${q}|${status}|${tag}\`})` — deleted the scattered `setLimit(BATCH)` calls in every filter `onChange` (the hook's `resetKey` now does that in one place).
   - Fixed the `min-h-0` → `min-h-0 flex-1` bug described above.
   - Email cell got `max-w-56 truncate` (wasn't truncating before — could theoretically two-line on long addresses).

3. **`app/(app)/directory/directory-client.tsx`** — fully done:
   - Removed `<Pagination>` entirely. Rewrote the fetch flow to accumulate: `load(pageToLoad, replace)` — `replace=true` on filter/tab change (resets `items`/`page` to page 1), `replace=false` from the sentinel (appends). A `useSentinel` drives `load(page+1, false)` when the last row scrolls into view and `hasMore && !loading && !loadingMore`.
   - **Known, intentional limitation** (the doc explicitly allows this): `searchProviders` has no `sort` param, so sort is client-side over whatever's been paged in so far — sorting doesn't reorder rows not yet loaded, it only re-sorts the accumulated set. Noted here per the doc's "note the limitation in your report" instruction — **make sure this makes it into the final report to Brendan**.
   - Two separate `useSort` instances (Providers tab: name/specialty/city; Programs tab: name/agency/county) since the two tabs have different row shapes. Both hooks are called unconditionally every render (rules of hooks) even though only one tab's table is visible at a time — cheap, harmless.
   - Checkbox "select all" semantics renamed from "select all on page" → "select all loaded" (`allLoadedChecked`/`toggleAllLoaded`) since there's no more "page" concept in the UI.
   - Added `min-w-0`/`max-w-*`/`truncate`/`title` treatment to Provider name+credential, Specialty, Sub-specialty, Address, City, Program name, Agency, Type, County — none of these truncated before.
   - Left the `NetworkSignal` sub-line under a provider's name **alone** (it's a deliberate two-line meta pattern — payer/accepting-status/as-of under the name — judged this isn't the "wrapped text" the doc's rule 5 is about; it's intentional secondary content, same category as a card subtitle. If Brendan pushes back on this in review, it's the one deliberate exception.)

4. **Rates — all three panels done** (`components/rates/bands-panel.tsx`, `panels-panel.tsx`, `spread-panel.tsx`), plus:
   - **`components/rates/rates-shell.tsx`**: root is now `flex h-full min-h-0 flex-col`. Bands and Panels tab bodies own their scroll internally (table-owned scroll, no `overflow-y-auto` on the wrapper div). **Spread's tab body is `overflow-y-auto`** — deliberately NOT table-owned-scroll, because SpreadPanel is a form-then-small-bounded-result screen (≤~25 payer rows), not an unbounded data table; forcing the same sticky/bounded-scroll treatment there fights the natural form-then-results flow. This is a judgment call, not a literal reading of the doc (which lists all three files under "apply the standard") — flag it in the report as a deliberate deviation with reasoning, in case Brendan disagrees.
   - **`components/rates/insurer-mark.tsx`**: `InsurerCell`'s payer-name span had no `truncate`/`max-width` at all — fixed (`max-w-[26rem] truncate` + `title`). This is shared by `BandsPanel` and `SpreadPanel`'s Insurer column, so fixing it once fixed both. (`PanelsPanel` already truncated its own inline insurer-name span — untouched.)
   - **Critical rule respected**: `lib/repos/rate-signals.ts`'s header comment (and the doc's ground rules) forbid unwrapping the repo's qualified display strings (e.g. `"$98.75 in-network"`, `"≈ +$21,900/yr"`) back into bare numbers. This directly constrained what could be made sortable:
     - `RateBand` has a **raw** `clinicians: number` and `asOf: string` (ISO, sorts correctly as a string) alongside the qualified `p25`/`median`/`p75` (strings) — so Bands' sortable columns are Insurer/Code/License/Clinicians/As-of; **P25/Median/P75/Schedule are intentionally NOT sortable** (no raw numeric field exists for them without parsing the display string, which the rule forbids).
     - Panels' `Row.rate` is a display string too (`r.display`) — same reasoning, **Rate is not sortable**; sortable columns are Clinician (multi-NPI only)/Insurer/Code/As-of/On TIN (`onTin` is a raw `number | null`).
     - Spread's `PayerSpread.annualDisplay` is explicitly documented in the repo type as "direction only — drives the tint, never reveals the figure" — so its sortable "Annualized" column sorts by the `positive: boolean` field, not by parsing the string.
   - `table-skeleton.tsx` needed **zero changes** — column count/order wasn't changed on any panel, only made interactive, and the skeleton takes plain label strings independently of the real table's head cells.
   - Lazy-batch (`useLazyBatch`) added to all three including Spread, where it's a functional no-op today (payer counts are well under the 50-row batch size) — added for structural consistency per "implement once," not because Spread currently needs it.

### All done — sections 5–8 below

5. **Billing restructure** (`components/billing/billing-shell.tsx` + `app/(app)/billing/page.tsx`) — **done**, built exactly to this plan:

   - **`billing-shell.tsx`** — full rewrite:
     - Tabs become **Overview · Clients · Payers** (currently Overview/Open/Settled/Payers).
     - **Kill the master-detail split entirely** (the `<aside>` invoice-list rail + right pane). `InvoicePane` (`components/billing/invoice-pane.tsx`) already has its own back button (`router.push("/billing")`) and its own 68px header — it was **already built to stand alone**, confirmed by reading it. So: when `activeId` is truthy (pathname is `/billing/[id]`), `BillingShell` should render **only** `<div className="flex h-full min-h-0 flex-col">{children}</div>` — no Tabs, no Toolbar, no TopBarActions, nothing else. That's "the full-page view" the doc's spec literally calls it.
     - Compute all hooks unconditionally before branching in the JSX (don't early-`return` before hook calls — React rules of hooks). Branch inside the returned JSX, not via an early return.
     - When `!activeId`: render Tabs + tab content inline (no more delegating to `{children}` for the list state — see next bullet on why `billing/page.tsx` becomes a stub).
     - **Overview tab**: KPI `StatCard` grid (unchanged) **plus** the "Needs attention" table, moved in-line. `billing-shell.tsx` already receives `invoices: InvoiceListItem[]` as a prop from the layout — compute the attention list there with a `useMemo` mirroring the existing `ATTENTION_ORDER`/`.slice(0,8)` logic currently in `app/(app)/billing/page.tsx` (copy that logic in, don't fetch again). Render via the existing `AttentionTable` component (`components/billing/attention-table.tsx`) unchanged — it already uses the `Table` primitive so it gets the teal header for free.
     - **Clients tab**: one merged invoices table (was Open + Settled, now one table with a status filter chip). Toolbar = SearchInput + a status `ChipMenu` (reuse `components/rates/chip-menu.tsx`'s `ChipMenu` — see reuse note below) with options built from `InvoiceStatus` (`draft/sent/paid/overdue/void`), each option's `lead` a `DotBadge` matching `InvoiceStatusBadge`'s variant map (draft=neutral, sent=info, paid=success, overdue=danger, void=neutral — copy this map, don't import from `invoice-status-badge.tsx` since it's component-scoped there). Columns: **Client · Invoice # · Issued · Due · Status · Balance** (per the doc, exact column list). Sortable on all six (client=clientName localeCompare, number=localeCompare, issued/due=the `YYYY-MM-DD` strings localeCompare directly — nulls as `??  ""` sorts them first, acceptable, don't over-engineer, status=label localeCompare or a small rank map, balance=raw `balanceCents` number — this field is already a raw number everywhere else in the app, no "bare number" rule concern here, that rule is specific to `lib/repos/rate-signals.ts`'s qualified strings, not billing). Row click → `router.push(`/billing/${inv.id}`)` (plain navigation now, no more `Link` into a preserved split state). `useLazyBatch` + `stickyHeader` + `min-h-0 flex-1` per the standard.
     - **Payers tab**: convert the existing `ListRow`-based list into a standard `Table` (sticky/lazy/sortable — sort by payerCode/name/policyCount). Keep the existing `KebabMenu` (Edit → opens `PayerPanel`, Delete → existing `deletePayer`) as the trailing cell. Row click could also open edit, or leave that to the kebab only (existing behavior had `ListRow` clickable? — check: original had no row `onClick`, only kebab menu actions — keep it that way, don't add a row click that isn't in the current UX).
     - `TopBarActions`: "New invoice" button only shows on Overview/Clients tabs; "New payer" only on Payers tab; **neither renders when `activeId` is set** (detail page owns its own actions inline — this was already true in the old code's structure, just make sure the gating survives the rewrite).
     - `NewInvoicePanel` and `PayerPanel` stay mounted at the shell level exactly as before (unaffected by this rewrite, they're modals/side-panels, not part of the killed split).
     - Equal-margins fix (`mb-4` → `mb-4 md:mb-6` on the Clients/Payers tab toolbars) same as every other page this session.

   - **`app/(app)/billing/page.tsx`** — simplify to a stub (`export const dynamic = "force-dynamic"; export default function BillingPage() { return null; }` or similar) since its content (the attention table) moved into `billing-shell.tsx` directly, which already has `invoices` from the layout — no need for `billing/page.tsx` to fetch `listInvoices()` again. **Do not delete the file** — Next.js needs a `page.tsx` in the segment for `/billing` to resolve as a route at all (it just won't render anything since `BillingShell` no longer references `children` when `!activeId`).

   - **Do NOT touch**: `app/(app)/billing/[id]/page.tsx`, `components/billing/invoice-pane.tsx`, `app/(app)/billing/[id]/print/*`, `components/billing/client-invoices.tsx` (the client-record Billing tab's own small invoice table — out of the doc's enumerated per-page scope, leave it), `lib/repos/invoices.ts` (no field is missing — `issuedOn` already exists on `InvoiceListItem`).

   - **Reuse note**: `components/rates/chip-menu.tsx`'s `ChipMenu` is a more capable, already-generic single/multi-select filter-chip component than the bespoke one duplicated inline in both `clients-index.tsx` and `directory-client.tsx`. Rather than write a 4th copy for Billing's status filter, import it directly from `@/components/rates/chip-menu` — yes, that's importing a "rates" file into "billing," which is a little odd organizationally, but it's the right call over forking a duplicate, and moving/renaming the file was judged out of scope (it'd touch import lines in `bands-panel.tsx`/`panels-panel.tsx`, which are uncommitted concurrent-session files — see "Working style" below). Mention this reuse decision in the final report.

6. **Portal sweep** (`app/portal/*`) — **done**. `app/portal/records/records-list.tsx`'s hand-rolled `sortHead()`/`toggleSort`/`Icon` chevron pattern was refactored to use the shared `useSort`/`SortableHead` from `@/components/ui/table` (dropped the local duplicate + the now-unused `Icon` import; already had `stickyHeader` + `min-h-0 flex-1` correctly set, no other change needed there). Everything else in `app/portal/*` (`invoices-list.tsx`, `appointments-list.tsx`, `resources-list.tsx`, `forms-grid.tsx`) was read in full and confirmed already compliant or correctly out-of-pattern by design (card grids / agenda lists with no `Pagination`, no double-scroll) — no changes made to those.

7. **Verification + report** — **done**:
   - `npx tsc --noEmit` — clean, final run after every section, zero errors.
   - `grep -rn "Pagination" app components` confirmed zero remaining consumers in the workspace/portal — the only hits left are `components/ui/pagination.tsx` itself (the primitive definition, untouched — doc didn't ask to delete it) and its `/design-system` catalog showcase card (expected), plus unrelated public-marketing-site usage (`app/programs/family/[slug]/page.tsx`, `components/site/program-controls.tsx` — out of scope, marketing pages).
   - **No headless-browser/Playwright verification was run and no screenshots were taken**, per Brendan's explicit standing instruction this session ("don't waste tokens on screenshots unless ABSOLUTELY NECESSARY, otherwise trust the code") — this reinforces existing memory (`feedback-surgical-mode.md` Addendum 3). The task brief's own "Verify on localhost:3010" line is satisfied by Brendan checking the dev server himself; flagged this resolution plainly in the final report rather than silently picking one.
   - `git log`/`git diff HEAD` checked explicitly for concurrent-session collisions before reporting — confirmed clean (see top of this doc). Final uncommitted diff: 11 files, +694/-475 (`git diff --stat`).
   - **Nothing was staged or committed** — that's for Brendan to do explicitly, and only the files this task touched, never `git add -A` (concurrent-session files are still dirty in the tree: `lib/repos/networks.ts`, scraping/`.harvest` scripts, etc. — not touched, not staged).

## Working style / standing instructions for this session (important, don't relitigate)

- **Never screenshot or run headless browser verification unless explicitly asked.** Brendan is watching the dev server live and considers it wasted tokens. Just make the edits, keep `tsc` clean, and report.
- **Never `git add -A`** — the tree has concurrent-session work in it (a DB/scraping terminal owns `/rates` data files, payer/insurance-filter files, `lib/repos/networks.ts`; another owns `note-sheet.tsx`/`app-shell.tsx`/`topbar.tsx`). Stage explicit file paths only.
- Don't ask permission before continuing reversible edit work — proceed.
- No commits have been made this session and none should be made without being asked.

## Quick orientation for whoever picks this up

- Read `docs/TASK-TABLE-STANDARD.md` (the spec) top to bottom first, then this file.
- The reference implementation the spec points to (`clients-index.tsx`) is now **already updated** by this session — don't re-read the old version, it's gone.
- `components/ui/table.tsx` is the single most important file to understand before touching anything else — every subsequent page leans on its exports.
- Dev server: `npm run dev` → localhost:3010, `brendan@liminal.demo` / `demo`.
